package org.elkoserver.foundation.server;

/* Note that this source file is a template.  The source that will actually be
   compiled for the BuildVersion class is generated from this template at build
   time by the Makefile. */

/**
 * Struct class, whose sole purpose is to hold onto a statically configured
 * version identification string.
 */
class BuildVersion {
    /** The version of Elko that this build represents. */
    static public String version = "@BuildVersion@";
}


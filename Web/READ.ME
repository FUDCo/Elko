chat.html works with a stand alone context server (use the
start.standalone.context.sh launching script) connected to the standard Elko
database. You will need to change the server name.

chatjq.html uses the JQuery connection library instead of older YUI.



